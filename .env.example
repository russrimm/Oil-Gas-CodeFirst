### Environment variable template (DO NOT COMMIT real secrets) ###
# Copy this file to .env and fill in values locally.

# Power Apps / Code App runtime (if needed for conditional logic)
VITE_POWERAPPS_ENVIRONMENT_URL=

# Copilot Studio / Bot integration (proxy layer)
COPILOT_CHAT_SERVICE_BASE_URL=

# Entra (Azure AD) App Registration (used ONLY server-side; never expose secret to client bundle)
ENTRA_CLIENT_ID=
ENTRA_TENANT_ID=
ENTRA_CLIENT_SECRET= # store only in secure server hosting environment, not for frontend build

# Copilot (front-end safe identifiers only; leave blank if not embedding)
VITE_COPILOT_APP_CLIENT_ID=
VITE_COPILOT_TENANT_ID=
VITE_COPILOT_ENVIRONMENT_ID=
VITE_COPILOT_AGENT_IDENTIFIER=
VITE_COPILOT_DIRECT_CONNECT_URL=

# Optional: Application Insights or custom telemetry keys
APPINSIGHTS_CONNECTION_STRING=

# Build-time feature flags
VITE_FEATURE_CHAT=1
VITE_FEATURE_3D=1